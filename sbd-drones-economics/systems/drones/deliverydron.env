# Delivery drone system identity (broker topics). Committed defaults; override locally with systems/deliverydron/.env (gitignored) if needed.
# Full component topic: ${TOPIC_VERSION}.${SYSTEM_NAME}.${INSTANCE_ID}.${COMPONENT_ID}
SYSTEM_NAME=deliverydron
TOPIC_VERSION=v1
INSTANCE_ID=Delivery001
